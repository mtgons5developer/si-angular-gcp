// app.routes.ts
import { Routes } from '@angular/router';
import { DashBoardComponent } from './dash-board/dash-board.component'; // Adjust the path as necessary

export const routes: Routes = [
  { path: 'dashboard', component: DashBoardComponent },
  // other routes if any
];
